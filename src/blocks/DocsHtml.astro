---
export interface Props {
  node: {
    html?: string;
  };
}

const { node } = Astro.props;
---

{node.html && <div class="emdash-block-wrap" data-block-type="docs.html" data-block-props={JSON.stringify(node)} data-sl-edit="html" set:html={node.html} />}
